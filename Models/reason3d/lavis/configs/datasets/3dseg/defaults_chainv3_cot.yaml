 # Defaults for chain v3 CoT (multi-step reasoning).
 # Mirrors defaults_chainv3.yaml; only the builder/dataset class differ.

datasets:
  3d_refer_chainv3_cot:
    data_type: [points]
    dataset_init:
      pth_rel_subdir: "processed_surprise_full_pth"
      # object_name_keys: ["object_name"]
      # max_object_name_chars: 96
      # chain_answer_fallback_plain: true
      # cot_template_prob: 1.0           # 1.0 = always emit CoT when landmark hits
      # cot_question_types: ["relative_position", "abs", "first_view"]
      # cot_target_phrase_template: "the {name}"

    build_info:
      annotations:
      points:
